# --- Core web framework ---
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-CORS==4.0.0
Werkzeug==3.0.1
python-dotenv==1.0.0

# --- Image handling (used for validation; required) ---
Pillow>=10.0.0
numpy>=1.24.0

# ----------------------------------------------------------------------
# Optional: AI inference dependencies (YOLOv8)
# The backend runs in "simulation mode" without these — every endpoint
# still works, classifications are randomized for development/testing.
# Uncomment to enable real AI inference:
# ----------------------------------------------------------------------
# ultralytics>=8.1.0
# opencv-python>=4.8.0
# torch>=2.0.0
# torchvision>=0.15.0
